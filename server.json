{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.singleflo/odoo-assistant",
  "description": "Odoo virtual employee via MCP: query, create, modify records, workflows, PDFs, and activities.",
  "repository": {
    "url": "https://github.com/singleflo/odoo-assistant-mcp",
    "source": "github"
  },
      "version": "0.3.2",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "odoo-assistant",
      "version": "0.3.2",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "ODOO_BASE_URL",
          "description": "Your Odoo instance URL (e.g. http://localhost:8069)",
          "isRequired": true,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_DB",
          "description": "Odoo database name. Optional: discovered automatically when the instance serves exactly one database; required when it serves several (the error names them).",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_USER",
          "description": "Odoo login username. Optional: omitted, the login is discovered from the API key.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_API_KEY",
          "description": "Odoo API key (Odoo 14+ required). Generate under Preferences / My Profile > Account Security > New API Key. Passwords are not accepted.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "ODOO_MCP_ALLOW",
          "description": "Methods the agent may call. `*` (default) allows every method not denied; `none` makes the server read-only; or a comma-separated list of `method` or `model:method` entries.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_MCP_DENY",
          "description": "Methods the agent may never call. A value set here REPLACES the default list, which is unlink,archive,action_cancel,button_cancel,action_reverse,action_draft,mailing.mailing:action_send.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_MCP_ALLOW_UNLINK",
          "description": "Deletion is the one action that cannot be undone: `yes` lets `unlink` through; nothing else can.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    }
  ],
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://mcp.singleflo.com/mcp"
    }
  ]
}
