# odoomon — generated reference

> Generated by `explore_module.py odoomon` on 2026-08-11 13:32 against `persevida_dev18`.
> Every figure below came from a call to this instance. Re-run to refresh.

Company context used: `{'allowed_company_ids': [2, 1]}`


## Models and volumes

| Model | Records | Fields | Menus/actions |
|---|---:|---:|---:|
| `odoomon.snapshot` | 4776 | 15 | 1 |
| `odoomon.module` | 3215 | 19 | 3 |
| `odoomon.path` | 339 | 9 | 1 |
| `odoomon.gh_module` | 175 | 11 | 1 |
| `odoomon.user` | 147 | 13 | 1 |
| `odoomon.repository` | 29 | 18 | 1 |

---

## `odoomon.snapshot`

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Odoo Monitor/Sync History | 4776 |  | `[]` |


---

## `odoomon.module`

### State fields — real values, read from the instance

`state`:

| key | label | records |
|---|---|---:|
| `installed` | Installed | 3215 |
| `uninstalled` | Not Installed | 0 |
| `uninstallable` | Uninstallable | 0 |
| `to_upgrade` | To Upgrade | 0 |
| `to_install` | To Install | 0 |
| `to_remove` | To Remove | 0 |

`github_status`:

| key | label | records |
|---|---|---:|
| `not_tracked` | Not Tracked | 2957 |
| `matched` | Matched | 258 |
| `removed` | Removed from GitHub | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Odoo Monitor/Updates Available | 34 |  | `[('update_available', '=', True)]` |
| Odoo Monitor/Removed from GitHub | 0 |  | `[('github_status', '=', 'removed')]` |


---

## `odoomon.path`


---

## `odoomon.gh_module`

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Odoo Monitor/GitHub/GitHub Modules | 175 |  | `[]` |


---

## `odoomon.user`

### State fields — real values, read from the instance

`user_type`:

| key | label | records |
|---|---|---:|
| `internal` | Internal | 147 |
| `portal` | Portal | 0 |
| `inactive` | Inactive | 0 |


---

## `odoomon.repository`

### State fields — real values, read from the instance

`repo_type`:

| key | label | records |
|---|---|---:|
| `custom` | Custom (Full History) | 29 |
| `oca` | OCA | 0 |
| `odoo` | Odoo Official | 0 |
| `other` | Other | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Odoo Monitor/GitHub/Repositories | 29 |  | `[('repo_type', '=', 'custom')]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_sync_modules  action_view_modules  button_box
```


---

## NOTES — hand-written, preserved across regenerations

Add pitfalls here as you hit them. This section survives
`explore_module.py` re-runs; everything above it does not.
