# projects — generated reference

> Generated by `explore_module.py projects` on 2026-08-11 13:42 against `persevida_dev18`.
> Every figure below came from a call to this instance. Re-run to refresh.

Company context used: `{'allowed_company_ids': [2, 1]}`


## Models and volumes

| Model | Records | Fields | Menus/actions |
|---|---:|---:|---:|
| `project.project` | 47 | 148 | 5 |
| `project.task` | 808 | 159 | 16 |
| `account.analytic.line` | 2191 | 68 | 17 |
| `documents.document` | 930 | 124 | 6 |

---

## `project.project`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`rating_status`:

| key | label | records |
|---|---|---:|
| `stage` | when reaching a given stage | 47 |
| `periodic` | on a periodic basis | 0 |

`rating_status_period`:

| key | label | records |
|---|---|---:|
| `daily` | Daily | 0 |
| `weekly` | Weekly | 0 |
| `bimonthly` | Twice a Month | 0 |
| `monthly` | Once a Month | 47 |
| `quarterly` | Quarterly | 0 |
| `yearly` | Yearly | 0 |

`last_update_status`:

| key | label | records |
|---|---|---:|
| `on_track` | On Track | 10 |
| `at_risk` | At Risk | 0 |
| `off_track` | Off Track | 0 |
| `on_hold` | On Hold | 0 |
| `to_define` | Set Status | 36 |
| `done` | Done | 1 |

`sale_order_state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 0 |
| `sent` | Quotation Sent | 0 |
| `sale` | Sales Order | 21 |
| `cancel` | Cancelled | 0 |

### `stage_id` → `project.project.stage` — the stage people mean by "open"

| stage | closed? | records |
|---|---|---:|
| Attesa | open | 7 |
| Questa settimana | open | 2 |
| Prioritario! | open | 3 |
| In Progress | open | 6 |
| Sospeso | open | 3 |
| To Do | open | 3 |
| Done | closed | 20 |
| Cancelled | closed | 3 |

**Open = `stage_id.fold = False` → 24 records, against 47 in total.** Reporting the total as the answer to "how many open" overstates it by 2.0×.

```python
odoo.search_count("project.project", [["stage_id.fold", "=", False]], CTX)
```

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Project/Projects | 43 |  | `[('is_internal_project', '=', False), ('helpdesk_team', '=',…` |
| Project/Projects | 43 |  | `[('is_internal_project', '=', False), ('helpdesk_team', '=',…` |
| Project/Configuration/Projects | 47 |  | `[]` |
| Project/Configuration/Projects | 47 |  | `[]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_customer_preview  action_open_share_project_wizard  action_view_sos  action_view_tasks  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `stage_id` → `project.project.stage`
- `user_id` → `res.users`


---

## `project.task`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`state`:

| key | label | records |
|---|---|---:|
| `01_in_progress` | In Progress | 717 |
| `02_changes_requested` | Changes Requested | 9 |
| `03_approved` | Approved | 5 |
| `1_done` | Done | 70 |
| `1_canceled` | Cancelled | 7 |
| `04_waiting_normal` | Waiting | 0 |

`repeat_type`:

| key | label | records |
|---|---|---:|
| `forever` | Forever | 0 |
| `until` | Until | 0 |

`sale_order_state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 0 |
| `sent` | Quotation Sent | 0 |
| `sale` | Sales Order | 512 |
| `cancel` | Cancelled | 0 |

`pricing_type`:

| key | label | records |
|---|---|---:|
| `task_rate` | Task rate | 0 |
| `fixed_rate` | Project rate | 0 |
| `employee_rate` | Employee rate | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Project/Tasks | ? |  | `[('is_closed', '=', False)]` |
| Project/Tasks/All Tasks | 670 | 731 ⚠️ 1.1× | `[('display_in_project', '=', True), ('is_closed', '=', False…` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_dependent_tasks  action_open_documents_portal  action_open_parent_task  action_open_ratings  action_recurring_tasks  action_view_documents_project_task  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `project_id` → `project.project`
- `stage_id` → `project.task.type`


---

## `account.analytic.line`

### State fields — real values, read from the instance

`validated_status`:

| key | label | records |
|---|---|---:|
| `draft` | Draft | 0 |
| `validated` | Validated | 0 |

`timesheet_invoice_type`:

| key | label | records |
|---|---|---:|
| `billable_time` | Billed on Timesheets | 1152 |
| `billable_fixed` | Billed at a Fixed price | 371 |
| `billable_milestones` | Billed on Milestones | 0 |
| `billable_manual` | Billed Manually | 6 |
| `non_billable` | Non-Billable | 508 |
| `timesheet_revenues` | Timesheet Revenues | 0 |
| `service_revenues` | Service Revenues | 0 |
| `other_revenues` | Other revenues | 137 |
| `other_costs` | Other costs | 17 |

`sale_order_state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 0 |
| `sent` | Quotation Sent | 0 |
| `sale` | Sales Order | 1506 |
| `cancel` | Cancelled | 17 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Timesheets/Timesheets/My Timesheets | ? |  | `[]` |
| Timesheets/My Timesheets | ? |  | `[]` |
| Timesheets/Timesheets/All Timesheets | 2037 |  | `[('project_id', '!=', False)]` |
| Timesheets/To Validate/Last Period | ? |  | `[]` |
| Timesheets/To Validate/All Timesheets | ? |  | `[]` |
| Accounting/Reporting/Management/Analytic Report | 2191 |  | `[]` |
| Accounting/Accounting/Analytic Items | 2191 |  | `[]` |

### Key relations

- `company_id` → `res.company`
- `employee_id` → `hr.employee`
- `order_id` → `sale.order`
- `partner_id` → `res.partner`
- `product_id` → `product.product`
- `project_id` → `project.project`
- `user_id` → `res.users`


---

## `documents.document`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`attachment_type`:

| key | label | records |
|---|---|---:|
| `url` | URL | 0 |
| `binary` | File | 862 |

`thumbnail_status`:

| key | label | records |
|---|---|---:|
| `present` | Present | 687 |
| `error` | Error | 0 |
| `client_generated` | Client Generated | 43 |
| `restricted` | Inaccessible | 0 |

`type`:

| key | label | records |
|---|---|---:|
| `url` | URL | 1 |
| `binary` | File | 862 |
| `folder` | Folder | 67 |

`create_activity_date_deadline_range_type`:

| key | label | records |
|---|---|---:|
| `days` | Days | 863 |
| `weeks` | Weeks | 0 |
| `months` | Months | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Documents/Documents | 930 |  | `[]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_archive  action_unarchive  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `product_id` → `product.product`
- `project_id` → `project.project`


---

## NOTES — hand-written, preserved across regenerations

Add pitfalls here as you hit them. This section survives
`explore_module.py` re-runs; everything above it does not.
