Security fix lane A — measured Final Wave blockers
Date: 2026-09-15

RED (before production edits)
Command:
  uv run pytest tests/test_remote_store.py tests/test_remote_consent.py tests/test_remote_files.py tests/test_remote_app.py tests/test_tenant.py
Result:
  10 failed, 56 passed, 9 errors
Measured failures:
  - pending pop race: 5 winners out of 20
  - authorization code race: 4 winners out of 20
  - confidential DCR secret present in raw SQLite bytes
  - 100.64.0.1 CGNAT target admitted (302)
  - verification timeout waited for the blocked worker and completed with 302
  - files.configure_data_dir absent; build_app wrote outside settings.data_dir
  - returned app exposed no stateless session-manager assertion seam
  - --help evaluated RemoteSettings.from_env first and raised
  - same subject with db-a/db-b reused one client
  - tenant.forget absent

GREEN (owned suites)
Command:
  uv run pytest tests/test_remote_store.py tests/test_remote_auth.py tests/test_remote_consent.py tests/test_remote_files.py tests/test_remote_app.py tests/test_tenant.py
Result:
  93 passed in 0.72s

GREEN (race repetition, native DELETE RETURNING and forced SQLite 3.34 fallback)
Command:
  for run in {1..15}; do uv run pytest -q \
    tests/test_remote_store.py::test_pending_pop_has_exactly_one_winner_under_concurrency \
    tests/test_remote_store.py::test_code_consume_has_exactly_one_winner_under_concurrency; done
Result:
  15 repetitions passed; each repetition races 20 threads against both the
  native path and forced BEGIN IMMEDIATE fallback. Exactly one winner each.
Runtime SQLite:
  sqlite3.sqlite_version = 3.53.1

GREEN (full repository suite)
Command:
  uv run pytest
Result:
  346 passed, 31 deselected in 4.72s

GREEN (quality gates)
  - uv run ruff check <all 12 owned Python files>: All checks passed
  - lsp_diagnostics on all six changed production modules: no errors
  - basedpyright executable is not installed in this project environment
  - no-excuse audit reported only inherited exceptions already present in the
    touched modules (documented SIZE_OK modules and pre-existing boundary/type
    patterns); no new ruff violation was introduced

Real CLI surface
Command:
  uv run odoo-assistant-remote --help
Result:
  usage: odoo-assistant-remote
  Run the hosted Odoo Assistant MCP server.

Artifact sweep — BEFORE
  ~/Library/Application Support/odoo-assistant/remote.db (61440 bytes)
  ~/Library/Application Support/odoo-assistant/files/t_seed/
  ~/Library/Application Support/odoo-assistant/files/t_configured/

Artifact sweep — AFTER rm of test-created t_seed*, t_configured*, remote.db*
  ~/Library/Application Support/odoo-assistant/files/ is empty
  no remote.db, remote.db-wal, or remote.db-shm remains
Post-test listing stayed empty, proving settings.data_dir injection is effective.

Implementation notes
  - SQLite >=3.35 uses DELETE ... RETURNING * for atomic pending/code consume.
    Older SQLite uses BEGIN IMMEDIATE around select/delete; it is exercised by
    tests and is not skipped.
  - Code exchange, refresh rotation, and family revocation hold BEGIN IMMEDIATE
    transactions. Fault-injection tests prove pair-write failures roll back the
    consume/revocation.
  - DCR JSON is Fernet-encrypted; authorization-code lookup keys are SHA-256.
  - Consent verification uses abandon_on_cancel=True. The HTTP request returns
    on timeout, while the uninterruptible worker thread may linger. The verified
    odoo_scripts are intentionally untouched; socket timeouts belong upstream.
  - There is no migration because this database has not shipped. Any pre-fix
    development remote.db should be deleted rather than retained.

{"DoneClaim":{"task":"Final Wave F2 security fixes","status":"done","tests":"346 passed, 31 deselected","race":"20 threads, exactly one winner on native and fallback paths","artifacts":"platform test artifacts removed; post-test listing empty","commit":"pending at evidence-write time","residual_risk":"abandoned credential-verification worker may linger until its socket call returns; canonical odoo_scripts were not modified"}}
