# evo_manager — generated reference

> Generated by `explore_module.py evo_manager` on 2026-08-11 13:33 against `persevida_dev18`.
> Every figure below came from a call to this instance. Re-run to refresh.

Company context used: `{'allowed_company_ids': [2, 1]}`


## Models and volumes

| Model | Records | Fields | Menus/actions |
|---|---:|---:|---:|
| `evolution.message` | 1537 | 60 | 1 |
| `res.partner` | 599 | 273 | 7 |

---

## `evolution.message`

### State fields — real values, read from the instance

`message_type`:

| key | label | records |
|---|---|---:|
| `conversation` | Text | 1278 |
| `extendedTextMessage` | Text with Link | 0 |
| `imageMessage` | Image | 61 |
| `videoMessage` | Video | 0 |
| `audioMessage` | Audio | 27 |
| `documentMessage` | Document | 53 |
| `stickerMessage` | Sticker | 40 |
| `locationMessage` | Location | 17 |
| `contactMessage` | Contact | 19 |
| `contactsArrayMessage` | Contacts | 0 |
| `reactionMessage` | Reaction | 0 |
| `pollCreationMessage` | Poll | 13 |

`status`:

| key | label | records |
|---|---|---:|
| `pending` | Pending | 20 |
| `server_ack` | Server ACK | 0 |
| `delivery_ack` | Delivered | 718 |
| `read` | Read | 798 |
| `played` | Played | 1 |
| `error` | Error | 0 |
| `deleted` | Deleted | 0 |

`content_type`:

| key | label | records |
|---|---|---:|
| `conversation` | Text | 1278 |
| `extendedTextMessage` | Text with Link | 0 |
| `imageMessage` | Image | 61 |
| `videoMessage` | Video | 0 |
| `audioMessage` | Audio | 27 |
| `documentMessage` | Document | 53 |
| `stickerMessage` | Sticker | 40 |
| `locationMessage` | Location | 17 |
| `contactMessage` | Contact | 19 |
| `contactsArrayMessage` | Contacts | 0 |
| `reactionMessage` | Reaction | 0 |
| `pollCreationMessage` | Poll | 13 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Evolution/Messaging/Messages | 1537 |  | `[]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_download_media  button_box
```


---

## `res.partner`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`type`:

| key | label | records |
|---|---|---:|
| `contact` | Contact | 567 |
| `invoice` | Invoice Address | 3 |
| `delivery` | Delivery Address | 1 |
| `facturae_ac` | FACe Center | 0 |
| `followup` | Follow-up Address | 0 |
| `other` | Other Address | 25 |

`company_type`:

| key | label | records |
|---|---|---:|
| `person` | Individual | 0 |
| `company` | Company | 0 |

`peppol_verification_state`:

| key | label | records |
|---|---|---:|
| `not_verified` | Unchecked | 583 |
| `not_valid` | Partner is not on Peppol | 16 |
| `not_valid_format` | Partner cannot receive format | 0 |
| `valid` | Partner is on Peppol | 0 |

`followup_status`:

| key | label | records |
|---|---|---:|
| `in_need_of_action` | In need of action | 0 |
| `with_overdue_invoices` | With overdue invoices | 0 |
| `no_action_needed` | No action needed | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Contacts/Contacts | 599 |  | `[]` |
| CRM/Sales/Customers | 599 |  | `[]` |
| Purchase/Orders/Vendors | 64 |  | `[('supplier_rank', '>', 0)]` |
| Sales/Orders/Customers | 179 |  | `[('customer_rank', '>', 0)]` |
| Subscriptions/Subscriptions/Customers | 179 |  | `[('customer_rank', '>', 0)]` |
| Accounting/Customers/Customers | 179 |  | `[('customer_rank', '>', 0)]` |
| Accounting/Vendors/Vendors | 64 |  | `[('supplier_rank', '>', 0)]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_view_opportunity  action_view_sale_order  button_box
```

### Key relations

- `company_id` → `res.company`
- `user_id` → `res.users`


---

## NOTES — hand-written, preserved across regenerations

Add pitfalls here as you hit them. This section survives
`explore_module.py` re-runs; everything above it does not.
