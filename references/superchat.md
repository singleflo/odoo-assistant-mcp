# superchat — generated reference

> Generated by `explore_module.py superchat` on 2026-08-11 13:32 against `persevida_dev18`.
> Every figure below came from a call to this instance. Re-run to refresh.

Company context used: `{'allowed_company_ids': [2, 1]}`


## Models and volumes

| Model | Records | Fields | Menus/actions |
|---|---:|---:|---:|
| `superchat.message` | 469 | 61 | 1 |
| `superchat.webhook.event` | 605 | 15 | 1 |
| `superchat.conversation.related.record` | 80 | 13 | 0 |
| `superchat.template` | 3 | 39 | 1 |
| `superchat.template.variable` | 35 | 18 | 0 |

---

## `superchat.message`

### State fields — real values, read from the instance

`status`:

| key | label | records |
|---|---|---:|
| `received` | Received | 106 |
| `sent` | Sent | 253 |
| `delivered` | Delivered | 0 |
| `read` | Read | 103 |
| `failed` | Failed | 0 |
| `sending_failed` | Sending Failed | 7 |
| `note` | Note | 0 |

`content_type`:

| key | label | records |
|---|---|---:|
| `text` | Text | 370 |
| `image` | Image | 0 |
| `video` | Video | 0 |
| `audio` | Audio | 0 |
| `file` | File | 0 |
| `media` | Media | 26 |
| `whats_app_template` | WhatsApp Template | 73 |
| `whats_app_quick_reply` | WhatsApp Quick Reply | 0 |
| `whats_app_list` | WhatsApp List | 0 |
| `email` | Email | 0 |
| `note` | Note | 0 |
| `other` | Other | 0 |

`message_type`:

| key | label | records |
|---|---|---:|
| `text` | Text | 370 |
| `image` | Image | 0 |
| `video` | Video | 0 |
| `audio` | Audio | 0 |
| `file` | File | 0 |
| `media` | Media | 26 |
| `whats_app_template` | WhatsApp Template | 73 |
| `whats_app_quick_reply` | WhatsApp Quick Reply | 0 |
| `whats_app_list` | WhatsApp List | 0 |
| `email` | Email | 0 |
| `note` | Note | 0 |
| `other` | Other | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| SuperChat/Configuration/Messages | ? |  | `[]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_download_media  action_open_contact  action_open_conversation  action_view_parent  action_view_replies  button_box
```

### Key relations

- `company_id` → `res.company`


---

## `superchat.webhook.event`

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| SuperChat/Configuration/Webhook Events | 605 |  | `[]` |


---

## `superchat.conversation.related.record`


---

## `superchat.template`

### State fields — real values, read from the instance

`status`:

| key | label | records |
|---|---|---:|
| `draft` | Draft | 0 |
| `approved` | Approved | 3 |
| `pending` | Pending | 0 |
| `submitted` | Submitted | 0 |
| `rejected` | Rejected | 0 |

`header_type`:

| key | label | records |
|---|---|---:|
| `text` | Text | 1 |
| `image` | Image | 0 |
| `video` | Video | 0 |
| `document` | Document | 0 |

`template_type`:

| key | label | records |
|---|---|---:|
| `whats_app_template` | WhatsApp Business Template | 3 |
| `generic_template` | Generic Template | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| SuperChat/Templates | 3 |  | `[]` |

### Key relations

- `company_id` → `res.company`
- `user_id` → `res.users`


---

## `superchat.template.variable`

### State fields — real values, read from the instance

`variable_type`:

| key | label | records |
|---|---|---:|
| `static` | Static | 35 |
| `dynamic` | Dynamic | 0 |
| `system` | System | 0 |
| `custom` | Custom | 0 |
| `contact_attribute` | Contact Attribute | 0 |


---

## NOTES — hand-written, preserved across regenerations

Add pitfalls here as you hit them. This section survives
`explore_module.py` re-runs; everything above it does not.
