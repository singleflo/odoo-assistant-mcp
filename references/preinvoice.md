# preinvoice — generated reference

> Generated by `explore_module.py preinvoice` on 2026-08-11 13:33 against `persevida_dev18`.
> Every figure below came from a call to this instance. Re-run to refresh.

Company context used: `{'allowed_company_ids': [2, 1]}`


## Models and volumes

| Model | Records | Fields | Menus/actions |
|---|---:|---:|---:|
| `account.move` | 3478 | 258 | 16 |
| `sale.order` | 227 | 199 | 17 |
| `sale.order.line` | 1215 | 101 | 1 |
| `payment.transaction` | 60 | 46 | 2 |
| `account.journal` | 9 | 99 | 2 |

---

## `account.move`

### Sum the `_signed` twin, never the plain field

These fields are in the record's own currency; the `_signed`
version is converted to company currency and sign-correct:

- `amount_residual` → use **`amount_residual_signed`**
- `amount_tax` → use **`amount_tax_signed`**
- `amount_total` → use **`amount_total_signed`**
- `amount_untaxed` → use **`amount_untaxed_signed`**

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`state`:

| key | label | records |
|---|---|---:|
| `draft` | Draft | 177 |
| `posted` | Posted | 3301 |
| `cancel` | Cancelled | 0 |

`move_type`:

| key | label | records |
|---|---|---:|
| `entry` | Journal Entry | 2433 |
| `out_invoice` | Customer Invoice | 373 |
| `out_refund` | Customer Credit Note | 16 |
| `in_invoice` | Vendor Bill | 653 |
| `in_refund` | Vendor Credit Note | 3 |
| `out_receipt` | Sales Receipt | 0 |
| `in_receipt` | Purchase Receipt | 0 |

`payment_state`:

| key | label | records |
|---|---|---:|
| `not_paid` | Not Paid | 2208 |
| `in_payment` | In Payment | 75 |
| `paid` | Paid | 841 |
| `partial` | Partially Paid | 8 |
| `reversed` | Reversed | 12 |
| `blocked` | Blocked | 0 |
| `invoicing_legacy` | Invoicing App Legacy | 0 |

`status_in_payment`:

| key | label | records |
|---|---|---:|
| `not_paid` | Not Paid | 2031 |
| `in_payment` | In Payment | 75 |
| `paid` | Paid | 841 |
| `partial` | Partially Paid | 8 |
| `reversed` | Reversed | 12 |
| `blocked` | Blocked | 0 |
| `invoicing_legacy` | Invoicing App Legacy | 0 |
| `draft` | Draft | 177 |
| `cancel` | Cancelled | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Accounting/Customers/Invoices | 373 | 389 ⚠️ 1.0× | `[('move_type', 'in', ['out_invoice', 'out_refund']), '|', ('…` |
| Accounting/Vendors/Bills | 653 | 656 ⚠️ 1.0× | `[('move_type', 'in', ['in_invoice', 'in_refund']), ('move_ty…` |
| Accounting/Accounting/Journal Entries | 3301 |  | `[('state', '=', 'posted')]` |
| Accounting/Customers/Credit Notes | 16 | 389 ⚠️ 24.3× | `[('move_type', 'in', ['out_invoice', 'out_refund']), ('move_…` |
| Accounting/Vendors/Refunds | 3 | 656 ⚠️ 218.7× | `[('move_type', 'in', ['in_invoice', 'in_refund']), ('move_ty…` |
| Accounting/Customers/Receipts | 0 |  | `[('move_type', '=', 'out_receipt')]` |
| Accounting/Customers/Pre-Invoices | 0 |  | `[('is_preinvoice', '=', True), ('move_type', '=', 'out_invoi…` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_activate_currency  action_add_from_catalog  action_automatic_entry  action_invoice_sent  action_open_asset_ids  action_open_business_doc  action_post  action_print_pdf  action_register_payment  action_reverse  action_update_fpos_values  action_view_source_sale_orders  button_box  button_cancel  button_draft  button_hash  button_request_cancel  button_set_checked
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `team_id` → `crm.team`
- `user_id` → `res.users`


---

## `sale.order`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 6 |
| `sent` | Quotation Sent | 21 |
| `sale` | Sales Order | 119 |
| `cancel` | Cancelled | 81 |

`invoice_status`:

| key | label | records |
|---|---|---:|
| `upselling` | Upselling Opportunity | 7 |
| `invoiced` | Fully Invoiced | 47 |
| `to invoice` | To Invoice | 57 |
| `no` | Nothing to Invoice | 116 |

`terms_type`:

| key | label | records |
|---|---|---:|
| `plain` | Add a Note | 227 |
| `html` | Add a link to a Web Page | 0 |

`l10n_it_origin_document_type`:

| key | label | records |
|---|---|---:|
| `purchase_order` | Purchase Order | 0 |
| `contract` | Contract | 0 |
| `agreement` | Agreement | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| CRM/Sales/My Quotations | ? |  | `[]` |
| Sales/To Invoice/Orders to Invoice | 57 |  | `[('invoice_status', '=', 'to invoice')]` |
| Sales/Orders/Quotations | ? |  | `[]` |
| Subscriptions/Subscriptions/Subscriptions | 0 | 69 ⚠️ 69.0× | `[('subscription_state', 'in', ['1_draft', '3_progress', '4_p…` |
| Sales/Orders/Orders | 119 |  | `[('state', 'not in', ('draft', 'sent', 'cancel'))]` |
| Sales/To Invoice/Orders to Upsell | 7 |  | `[('invoice_status', '=', 'upselling')]` |
| Subscriptions/Subscriptions/Quotations | ? | 79 | `[('subscription_state', 'in', ['1_draft', '2_renewal', '3_pr…` |
| Subscriptions/Subscriptions/To Renew | ? | 79 | `[('subscription_state', '!=', False)]` |
| Subscriptions/Subscriptions/Upsells | 0 |  | `[('subscription_state', '=', '7_upsell')]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_add_from_catalog  action_cancel  action_confirm  action_draft  action_lock  action_open_discount_wizard  action_preview_sale_order  action_quotation_send  action_sale_order_log  action_unlock  action_update_prices  action_update_taxes  action_view_invoice  action_view_milestone  action_view_project_ids  action_view_task  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `project_id` → `project.project`
- `team_id` → `crm.team`
- `user_id` → `res.users`


---

## `sale.order.line`

### State fields — real values, read from the instance

`state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 21 |
| `sent` | Quotation Sent | 98 |
| `sale` | Sales Order | 460 |
| `cancel` | Cancelled | 636 |

`display_type`:

| key | label | records |
|---|---|---:|
| `line_section` | Section | 303 |
| `line_note` | Note | 187 |

`invoice_status`:

| key | label | records |
|---|---|---:|
| `upselling` | Upselling Opportunity | 8 |
| `invoiced` | Fully Invoiced | 287 |
| `to invoice` | To Invoice | 133 |
| `no` | Nothing to Invoice | 787 |

`product_type`:

| key | label | records |
|---|---|---:|
| `consu` | Goods | 77 |
| `service` | Service | 634 |
| `combo` | Combo | 0 |

### Key relations

- `company_id` → `res.company`
- `order_id` → `sale.order`
- `product_id` → `product.product`
- `project_id` → `project.project`


---

## `payment.transaction`

### State fields — real values, read from the instance

`state`:

| key | label | records |
|---|---|---:|
| `draft` | Draft | 12 |
| `pending` | Pending | 25 |
| `authorized` | Authorized | 0 |
| `done` | Confirmed | 19 |
| `cancel` | Canceled | 0 |
| `error` | Error | 4 |

`renewal_state`:

| key | label | records |
|---|---|---:|
| `draft` | Draft | 0 |
| `pending` | Pending | 0 |
| `authorized` | Authorized | 0 |
| `cancel` | Refused | 0 |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_capture  action_view_invoices  action_view_refunds  action_view_sales_orders  action_void  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`


---

## `account.journal`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 1 |
| `today` | Today | 0 |
| `planned` | Planned | 2 |

`type`:

| key | label | records |
|---|---|---:|
| `sale` | Sales | 1 |
| `purchase` | Purchase | 1 |
| `cash` | Cash | 1 |
| `bank` | Bank | 3 |
| `credit` | Credit Card | 0 |
| `general` | Miscellaneous | 3 |

`invoice_reference_type`:

| key | label | records |
|---|---|---:|
| `none` | Open | 0 |
| `partner` | Based on Customer | 0 |
| `invoice` | Based on Invoice | 9 |

`bank_statements_source`:

| key | label | records |
|---|---|---:|
| `undefined` | Ancora da definire | 6 |
| `file_import` | Manuale (o importa CAMT, CSV, JPEG, OFX, PDF, PNG, XLS, XLSX) | 0 |
| `online_sync` | Sincronizzazione online | 3 |

`account_online_link_state`:

| key | label | records |
|---|---|---:|
| `connected` | Connected | 3 |
| `error` | Error | 0 |
| `disconnected` | Not Connected | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Accounting/Dashboard | 7 |  | `[('show_on_dashboard', '=', True)]` |
| Accounting/Configuration/Accounting/Journals | 9 |  | `[]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_open_provider_form  action_send_reminder  button_box
```

### Key relations

- `company_id` → `res.company`


---

## NOTES — hand-written, preserved across regenerations

Add pitfalls here as you hit them. This section survives
`explore_module.py` re-runs; everything above it does not.
