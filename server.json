{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.singleflo/odoo-assistant",
  "description": "Odoo virtual employee via MCP: query, create, modify records, workflows, PDFs, and activities.",
  "repository": {
    "url": "https://github.com/singleflo/odoo-assistant-mcp",
    "source": "github"
  },
  "version": "0.1.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "odoo-assistant",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "ODOO_BASE_URL",
          "description": "Your Odoo instance URL (e.g. http://localhost:8069)",
          "isRequired": true,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_DB",
          "description": "Odoo database name. Optional: discovered automatically when the instance serves exactly one database; required when it serves several (the error names them).",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_USER",
          "description": "Odoo login username. Optional: omitted, the login is discovered from the API key.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_API_KEY",
          "description": "Odoo API key (Odoo 14+ required). Generate under Preferences / My Profile > Account Security > New API Key. Passwords are not accepted.",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "ODOO_MCP_MAX_LEVEL",
          "description": "Safety ceiling 0-5, default 3 (state transitions allowed, destructive ops blocked). See server_safety.py.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "ODOO_MCP_PROTECTED_HOSTS",
          "description": "Comma-separated hosts this server refuses to WRITE to (requires ODOO_ALLOW_PROD_WRITE=yes to override). Empty by default: protection is the operator's opt-in, no host is baked in.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    }
  ]
}
