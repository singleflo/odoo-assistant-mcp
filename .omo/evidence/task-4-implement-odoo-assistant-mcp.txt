# Task 4 evidence — corrected global installation

Correction:
The opencode harness discovers skills from ~/.agents/skills/, not the project-local .agents/skills/ directory.

Removed:
.agents/skills/ (and empty .agents/ directory)

Global installs completed with:
npx skills add anthropics/skills --skill mcp-builder -g -y
npx skills add mattpocock/skills --skill setup-pre-commit -g -y
npx skills add mattpocock/skills --skill tdd -g -y

Verified global paths:
~/.agents/skills/mcp-builder
~/.agents/skills/setup-pre-commit
~/.agents/skills/tdd

Verified ~/.agents/.skill-lock.json contains entries for mcp-builder, setup-pre-commit, and tdd.
Verified no fastmcp-* matches in the repository or ~/.agents/skills/.
Future installs for this session/project must use -g.
