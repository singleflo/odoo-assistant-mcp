# multi_mail — generated reference

> Generated by `explore_module.py multi_mail` on 2026-08-11 13:33 against `persevida_dev18`.
> Every figure below came from a call to this instance. Re-run to refresh.

Company context used: `{'allowed_company_ids': [2, 1]}`


## Models and volumes

| Model | Records | Fields | Menus/actions |
|---|---:|---:|---:|
| `helpdesk.ticket` | 53 | 129 | 13 |
| `sale.order` | 227 | 199 | 17 |
| `crm.lead` | 13 | 137 | 17 |

---

## `helpdesk.ticket`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`kanban_state`:

| key | label | records |
|---|---|---:|
| `normal` | In progress | 53 |
| `done` | Ready | 0 |
| `blocked` | Blocked | 0 |

`sale_order_state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 0 |
| `sent` | Quotation Sent | 0 |
| `sale` | Sales Order | 22 |
| `cancel` | Cancelled | 0 |

### `stage_id` → `helpdesk.stage` — the stage people mean by "open"

| stage | closed? | records |
|---|---|---:|
| New | open | 3 |
| In Progress | open | 1 |
| On Hold | open | 2 |
| Solved | closed | 45 |
| Cancelled | closed | 2 |

**Open = `stage_id.fold = False` → 6 records, against 53 in total.** Reporting the total as the answer to "how many open" overstates it by 8.8×.

```python
odoo.search_count("helpdesk.ticket", [["stage_id.fold", "=", False]], CTX)
```

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| Helpdesk/Tickets/My Tickets | ? |  | `[('stage_id.fold', '=', False)]` |
| Helpdesk/Tickets/All Tickets | 6 |  | `[('stage_id.fold', '=', False)]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_convert_ticket_to_lead_or_opportunity  action_open_helpdesk_ticket  action_open_ratings  action_timer_pause  action_timer_resume  action_timer_start  action_timer_stop  action_view_invoices  action_view_so  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `project_id` → `project.project`
- `stage_id` → `helpdesk.stage`
- `team_id` → `helpdesk.team`
- `user_id` → `res.users`


---

## `sale.order`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`state`:

| key | label | records |
|---|---|---:|
| `draft` | Quotation | 6 |
| `sent` | Quotation Sent | 21 |
| `sale` | Sales Order | 119 |
| `cancel` | Cancelled | 81 |

`invoice_status`:

| key | label | records |
|---|---|---:|
| `upselling` | Upselling Opportunity | 7 |
| `invoiced` | Fully Invoiced | 47 |
| `to invoice` | To Invoice | 57 |
| `no` | Nothing to Invoice | 116 |

`terms_type`:

| key | label | records |
|---|---|---:|
| `plain` | Add a Note | 227 |
| `html` | Add a link to a Web Page | 0 |

`l10n_it_origin_document_type`:

| key | label | records |
|---|---|---:|
| `purchase_order` | Purchase Order | 0 |
| `contract` | Contract | 0 |
| `agreement` | Agreement | 0 |

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| CRM/Sales/My Quotations | ? |  | `[]` |
| Sales/To Invoice/Orders to Invoice | 57 |  | `[('invoice_status', '=', 'to invoice')]` |
| Sales/Orders/Quotations | ? |  | `[]` |
| Subscriptions/Subscriptions/Subscriptions | 0 | 69 ⚠️ 69.0× | `[('subscription_state', 'in', ['1_draft', '3_progress', '4_p…` |
| Sales/Orders/Orders | 119 |  | `[('state', 'not in', ('draft', 'sent', 'cancel'))]` |
| Sales/To Invoice/Orders to Upsell | 7 |  | `[('invoice_status', '=', 'upselling')]` |
| Subscriptions/Subscriptions/Quotations | ? | 79 | `[('subscription_state', 'in', ['1_draft', '2_renewal', '3_pr…` |
| Subscriptions/Subscriptions/To Renew | ? | 79 | `[('subscription_state', '!=', False)]` |
| Subscriptions/Subscriptions/Upsells | 0 |  | `[('subscription_state', '=', '7_upsell')]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_add_from_catalog  action_cancel  action_confirm  action_draft  action_lock  action_open_discount_wizard  action_preview_sale_order  action_quotation_send  action_sale_order_log  action_unlock  action_update_prices  action_update_taxes  action_view_invoice  action_view_milestone  action_view_project_ids  action_view_task  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `project_id` → `project.project`
- `team_id` → `crm.team`
- `user_id` → `res.users`


---

## `crm.lead`

### State fields — real values, read from the instance

`activity_state`:

| key | label | records |
|---|---|---:|
| `overdue` | Overdue | 0 |
| `today` | Today | 0 |
| `planned` | Planned | 0 |

`type`:

| key | label | records |
|---|---|---:|
| `lead` | Lead | 0 |
| `opportunity` | Opportunity | 13 |

`phone_state`:

| key | label | records |
|---|---|---:|
| `correct` | Correct | 3 |
| `incorrect` | Incorrect | 1 |

`email_state`:

| key | label | records |
|---|---|---:|
| `correct` | Correct | 12 |
| `incorrect` | Incorrect | 0 |

`won_status`:

| key | label | records |
|---|---|---:|
| `won` | Won | 1 |
| `lost` | Lost | 0 |
| `pending` | Pending | 12 |

### `stage_id` → `crm.stage` — the stage people mean by "open"

| stage | closed? | records |
|---|---|---:|
| New | open | 2 |
| Qualified | open | 2 |
| Proposition | open | 5 |
| Bassa Probabilità | open | 3 |
| Won | open | 1 |

**Open = `stage_id.fold = False` → 13 records, against 13 in total.** Reporting the total as the answer to "how many open" overstates it by 1.0×.

```python
odoo.search_count("crm.lead", [["stage_id.fold", "=", False]], CTX)
```

### What the user sees in the menus

The count is the **resolved** one (action domain + the menu's
`search_default_*`). Where they differ, the naive figure is what
a query on the action domain alone would wrongly report.

| Menu | Records | Naive | Domain |
|---|---:|---:|---|
| CRM/Sales/My Activities | ? | 0 | `[('active', 'in', [True, False]), ('activity_ids.active', 'i…` |
| CRM/Reporting/Pipeline | 13 |  | `[('type', '=', 'opportunity')]` |
| CRM/Reporting/Leads | 0 |  | `[('active', '=', True), ('active', '=', False)]` |
| CRM/Leads | 0 |  | `['|', ('type', '=', 'lead'), ('type', '=', False)]` |

### Action methods found in the form views

These are the write surface. Classify before calling — see
`safety_layer.py`; anything not whitelisted is refused.

```
action_assign_partner  action_sale_quotations_new  action_schedule_meeting  action_set_automated_probability  action_set_won_rainbowman  action_show_potential_duplicates  action_view_sale_order  action_view_sale_quotation  button_box
```

### Key relations

- `company_id` → `res.company`
- `partner_id` → `res.partner`
- `stage_id` → `crm.stage`
- `team_id` → `crm.team`
- `user_id` → `res.users`


---

## NOTES — hand-written, preserved across regenerations

Add pitfalls here as you hit them. This section survives
`explore_module.py` re-runs; everything above it does not.
